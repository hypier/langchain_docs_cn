# 腾讯

>[腾讯控股有限公司 (维基百科)](https://en.wikipedia.org/wiki/Tencent) (中文: 腾讯; 拼音: Téngxùn) 
> 是一家总部位于深圳的中国跨国科技集团和控股公司。`Tencent` 是全球收入最高的多媒体公司之一。它也是全球视频游戏行业中基于股权投资的最大公司。

## 聊天模型

>[腾讯的混合模型API](https://cloud.tencent.com/document/product/1729) (`Hunyuan API`) 
> 实现了对话通信、内容生成、分析和理解，广泛应用于智能客服、智能营销、角色扮演、广告、文案撰写、产品描述、剧本创作、简历生成、文章写作、代码生成、数据分析和内容分析等各种场景。

有关更多信息，请参见[此笔记本](/docs/integrations/chat/tencent_hunyuan)

```python
from langchain_community.chat_models import ChatHunyuan
```

## 文档加载器

### 腾讯云 COS

>[腾讯云对象存储 (COS)](https://www.tencentcloud.com/products/cos) 是一种分布式存储服务，允许您通过 HTTP/HTTPS 协议从任何地方存储任意数量的数据。 
> `COS` 对数据结构或格式没有限制。它也没有桶大小限制和分区管理，使其适合几乎所有用例，例如数据传输、 
> 数据处理和数据湖。COS 提供基于网页的控制台、多语言 SDK 和 API、 
> 命令行工具以及图形工具。它与 Amazon S3 API 兼容，使您能够快速访问社区工具和插件。

安装 Python SDK：

```bash
pip install cos-python-sdk-v5
```

#### 腾讯云 COS 目录

有关更多信息，请参见 [此笔记本](/docs/integrations/document_loaders/tencent_cos_directory)

```python
from langchain_community.document_loaders import TencentCOSDirectoryLoader
from qcloud_cos import CosConfig
```

#### 腾讯云 COS 文件

有关更多信息，请参见 [此笔记本](/docs/integrations/document_loaders/tencent_cos_file)

```python
from langchain_community.document_loaders import TencentCOSFileLoader
from qcloud_cos import CosConfig
```

## 向量存储

### 腾讯 VectorDB

>[腾讯云 VectorDB](https://www.tencentcloud.com/products/vdb) 是一款完全托管的自研企业级分布式数据库服务，专门用于存储、检索和分析多维向量数据。该数据库支持多种索引类型和相似度计算方法，单个索引支持 10 亿个向量、数百万 QPS 和毫秒级查询延迟。`腾讯云向量数据库`不仅可以为大型模型提供外部知识库，提高大型模型答案的准确性，还可以广泛应用于推荐系统、自然语言处理服务、计算机视觉和智能客服等 AI 领域。

安装 Python SDK：

```bash
pip install tcvectordb
```

有关更多信息，请参见 [此笔记本](/docs/integrations/vectorstores/tencentvectordb)

```python
from langchain_community.vectorstores import TencentVectorDB
```

## 聊天加载器

### 微信

>[微信](https://www.wechat.com/) 或者在中文中称为 `Weixin` 是由 `腾讯` 开发的一款中国即时通讯、社交媒体和移动支付应用。

查看 [使用示例](/docs/integrations/chat_loaders/wechat)。