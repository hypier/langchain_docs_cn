# Writer

本页面介绍如何在 LangChain 中使用 Writer 生态系统。
它分为两个部分：安装和设置，然后是对特定 Writer 包装器的引用。

## 安装与设置
- 获取 Writer API 密钥并将其设置为环境变量 (`WRITER_API_KEY`)

## 包装器

### LLM

存在一个 Writer LLM 包装器，您可以通过以下方式访问它
```python
from langchain_community.llms import Writer
```